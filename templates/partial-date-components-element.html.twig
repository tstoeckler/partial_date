{#
/**
 * @file
 * Adds a "container-inline" wrapper for partial date estimates element.
 *
 * Available variables:
 * - element: built by PartialDateEstimatesElement.
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('container-inline') }}>{{ element }}</div>
